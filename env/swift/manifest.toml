version = 1

[install]
swift.pkg-path = "swift"
cmake.pkg-path = "cmake"

[hook]
on-activate = '''
PROJECT_ROOT="$(cd "$FLOX_ENV_PROJECT/../.." && pwd)"
source "$PROJECT_ROOT/scripts/env/toolchain/common.sh"
export HYBRID_AI_PROFILE=${HYBRID_AI_PROFILE:-swift-dev}
'''
